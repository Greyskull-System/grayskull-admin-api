PORT=4000
# URL da API Admin Modulys Pax (tenant-auth)
MODULYS_PAX_ADMIN_API_URL=http://localhost:3000/api/admin
# Código do tenant (preenchido na geração)
MODULYS_PAX_TENANT_CODE=__TENANT_CODE__
# Mesmo JWT_SECRET usado pela Admin API para validar o token do tenant
MODULYS_PAX_JWT_SECRET=change-me-same-as-admin-api
# URLs dos serviços Core e Chat (Modulys Pax)
CORE_SERVICE_URL=http://localhost:3001
# Chat HTTP + WebSocket. No frontend use NEXT_PUBLIC_CHAT_WS_URL com esta mesma base (ex: http://localhost:9001) para tempo real.
CHAT_SERVICE_URL=http://localhost:9001
